# هيكل النظام (Schema Driven Architecture)

يعتمد نظام العيادات (Clinic System) على فلسفة **"الهيكل هو القائد" (Schema is the Leader)**. هذا يعني أن ملف `clinic_schema.json` ليس مجرد ملف تعريف لقاعدة البيانات، بل هو العقل المدبر الذي يقود النظام بأكمله: من بناء الجداول في الـ Backend، إلى توليد واجهات المستخدم في الـ Frontend، وانتهاءً بتعريف العلاقات والتقارير.

## الفلسفة: Schema Only Source of Truth

في هذا النظام، لا نقوم بكتابة كود لكل حقل أو جدول بشكل منفصل. بدلاً من ذلك، نقوم بتعريف كل شيء في ملف الهيكل (Schema)، والنظام يتكفل بالباقي.

- **قاعدة البيانات (Database):** يتم توليد الجداول (Tables)، الأعمدة (Columns)، والعلاقات (Relations) تلقائياً بناءً على التعريف في الهيكل.
- **الواجهة الأمامية (Frontend):** يتم رسم النماذج (Forms)، الجداول (Grids)، والقوائم المنسدلة (Dropdowns) ديناميكياً بقراءة هذا الهيكل.
- **المنطق (Logic):** قواعد العرض (Display Rules)، الترتيب، وحتى الأيقونات والألوان تأتي من الهيكل.

## مكونات الهيكل الأساسية

يتكون ملف الهيكل من عدة أقسام رئيسية تعمل بتناغم:

### 1. أنواع الجداول (Table Types)

تصنيف عالي المستوى للجداول لتنظيمها في الواجهة.

- **الإعدادات (`settings`)**: جداول التعريفات الأساسية (مثل: العيادات، الأطباء، الخدمات). وتتميز بأيقونة ⚙️.
- **الحركات (`operations`)**: العمليات اليومية (مثل: الحجوزات، التعاقدات، الفواتير). وتتميز بأيقونة 📋.
- **التقارير (`reports`)**: جداول مخصصة للتحليل والعرض. وتتميز بأيقونة 📊.
- **السجلات (`logs`)**: سجلات التدقيق والمتابعة. وتتميز بأيقونة 📜.

### 2. الموديولات (Modules)

تجميع منطقي للجداول لتشكيل القوائم الجانبية وسير العمل.

- **النظام الأساسي (`core`)**: الهيكل التنظيمي (الشركات، الفروع) والموارد.
- **الرعاية الصحية (`healthcare`)**: صلب عمل العيادة (العيادات، الأقسام، الطاقم الطبي).
- **العمليات (`operations`)**: تدفق العمل اليومي (المرضى، المواعيد).
- **المالية (`financial`)**: الفواتير، التسعير، والمعاملات.
- **النظام (`system`)**: إعدادات النظام واللغات (i18n).

### 3. وصف الجداول (Tables Meta)

ربط كل جدول بنوعه وأيقونته المميزة. هذا يسهل على الواجهة معرفة كيفية عرض الجدول في القوائم.
- مثال: `clinic_patients` يتبع نوع `operations` وأيقونته 🧍.

### 4. تعريف الجداول (Tables Definitions)

هذا هو القسم الأضخم والأهم، حيث يتم تعريف كل جدول بالتفصيل:

#### أ. الحقول (Fields)

تعريف دقيق لكل عمود:

- `name`: اسم الحقل البرمجي.
- `type`: نوع البيانات (`uuid`, `nvarchar`, `int`, etc).
- `labels`: التسمية الظاهرة للمستخدم (عربي/إنجليزي).
- `smart_features`: خصائص ذكية مثل `is_searchable` (قابل للبحث)، `is_edit_show` (يظهر في التعديل).

#### ب. العلاقات (Relations)

الربط بين الجداول (Foreign Keys). النظام يفهم هذه العلاقات ويوفر أدوات للتعامل معها، مثل جلب البيانات المرتبطة تلقائياً (Auto Hydration).

#### ج. الخصائص الذكية (Smart Features)

إعدادات إضافية تمنح الجدول "ذكاءً":

- `icon` & `colors`: الهوية البصرية.
- `display_rule`: كيف يظهر اسم السجل في القوائم (مثلاً: الاسم + الكود).
- `groups`: تقسيم حقول النموذج إلى مجموعات منطقية (Tabs/Groups) لسهولة الإدخال (مثلاً: "البيانات الشخصية"، "بيانات الاتصال").

## الخلاصة

بفضل هذا الهيكل، عملية التطوير تصبح:

1. **أسرع:** إضافة حقل جديد تتطلب تعديلاً بسيطاً في JSON فقط.
2. **أدق:** لا يوجد تعارض بين الـ Back والـ Front لأن المصدر واحد.
3. **أذكى:** النظام يفهم البيانات وعلاقاتها، مما يسمح بميزات متقدمة مثل البحث العميق والتقارير التلقائية.
